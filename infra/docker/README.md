# Docker infrastructure

The Supabase CLI owns the local PostgreSQL Docker lifecycle for Phase 2. Do not add a parallel Compose database or manually mutate the Supabase container.

Future non-Supabase service orchestration may be added here only in its roadmap phase.
